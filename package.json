{
  "name": "@nftmart/subql",
  "version": "1.0.3",
  "description": "nftmart subquery project",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "test": "jest",
    "prepack": "rm -rf dist && yarn build",
    "codegen": "subql codegen",
    "start": "subql-node -f . --local"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "btwiuse",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^8",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  },
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  }
}
