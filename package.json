{
  "name": "nftmart-query",
  "version": "1.0.0",
  "description": "nftmart graphql server",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/nftt-studio/nftmart-query",
  "repository": "github.com/nftt-studio/nftmart-query",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "btwiuse",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^3",
    "@subql/types": "^0.6.0",
    "typescript": "^4.1.3",
    "@subql/cli": "^0.7.3"
  }
}
